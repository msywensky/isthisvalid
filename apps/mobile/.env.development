# Simulator/emulator only — localhost resolves to the dev machine there.
# On a physical device, localhost does not reach your dev machine; override
# with your machine's LAN IP instead, e.g. http://192.168.1.23:3000
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
