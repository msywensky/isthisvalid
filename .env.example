# ────────────────────────────────────────────────────────────────────────────
# IsThisValid.com — Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to source control.
# ────────────────────────────────────────────────────────────────────────────

# ── Anthropic (required for Text / SMS scam detector) ────────────────────────
# Get a key at https://console.anthropic.com
# IMPORTANT: Set a monthly spend cap in the Anthropic console before going live.
# Anthropic console → Settings → Billing → Usage limits
ANTHROPIC_API_KEY=sk-ant-...

# ── Emailable API ────────────────────────────────────────────────────────────
# Get a free key at https://emailable.com — 250 verifications/month on the
# free tier. Leave blank to use local regex + disposable-domain checks only.
EMAILABLE_API_KEY=

# ── Google AdSense ──────────────────────────────────────────────────────────
# Your publisher ID from https://adsense.google.com
# Format: ca-pub-XXXXXXXXXXXXXXXXX
# Leave blank until your site is approved.
NEXT_PUBLIC_ADSENSE_ID=

# ── Optional: Google Analytics ───────────────────────────────────────────────
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# ── Rate limiting ────────────────────────────────────────────────────────────
# Upstash Redis for serverless rate limiting (20 req/min per IP).
# Create a free database at https://upstash.com, then copy the REST URL + token.
# Leave blank to disable rate limiting (fine for local dev).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ── Google Safe Browsing ─────────────────────────────────────────────────────
# Enables malware and phishing detection for URL checks (free, 10 000 req/day).
# 1. Go to https://console.cloud.google.com/
# 2. Enable the "Safe Browsing API"
# 3. Create an API key under APIs & Services → Credentials
# Leave blank to skip Safe Browsing (local URL checks still run).
GOOGLE_SAFE_BROWSING_API_KEY=

# ── Affiliate Links ──────────────────────────────────────────────────────────
# Tracking URLs for monetization partners. Set to your affiliate tracking links.
# These are public values (NEXT_PUBLIC_*) shown in client components.
NEXT_PUBLIC_ZEROBOUNCE_AFFILIATE_URL=https://aff.zerobounce.net/your_affiliate_id
NEXT_PUBLIC_NORDVPN_AFFILIATE_URL=https://go.nordvpn.net/aff_c?offer_id=15&aff_id=YOUR_AFF_ID
